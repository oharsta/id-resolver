# Valid scopes are resolver.api.Scope.values(); =>  ["READ", "WRITE"]
apiUsers:
  {% for user in manage.apiUsers %}
  - {
      name: "{{ user.name }}",
      password: "{{ user.password }}",
      scopes: [ {{ user.scopes|join(', ') }} ]
    }
  {% endfor %}
